"use client";

// Used to manage loading state and UI between routes inside the id directory, shows a loading directory in place of the page, when the data is being fetched
export default function Loading() {
  return <p>Loading...</p>;
}
